[
  {
    "cpu": 256,
    "essential": true,
    "image": "${image_url}",
    "memory": 512,
    "name": "${wr_container_name}",
    "portMappings": [
      {
        "containerPort": ${wr_port_num},
        "hostPort": ${wr_port_num}
      }
    ]
  }
]
