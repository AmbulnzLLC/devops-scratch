[
  {
    "cpu": 256,
    "essential": true,
    "image": "${image_url}",
    "memory": 512,
    "name": "${wr_container_name}",
    "portMappings": [
      {
        "containerPort": ${wr_port_num},
        "hostPort": ${wr_port_num}
      }
    ]
  },
  {
    "cpu": 256,
    "essential": true,
    "image": "${image_url}",
    "memory": 512,
    "name": "${api_container_name}",
    "portMappings": [
      {
        "containerPort": ${api_port_num},
        "hostPort": ${api_port_num}
      }
    ]
  },
  {
    "cpu": 256,
    "essential": true,
    "image": "${image_url}",
    "memory": 512,
    "name": "${relay_container_name}",
    "portMappings": [
      {
        "containerPort": ${relay_port_num},
        "hostPort": ${relay_port_num}
      }
    ]
  }
]
